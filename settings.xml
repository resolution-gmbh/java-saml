<?xml version="1.0" encoding="UTF-8"?>
<settings>
    <servers>
        <server>
            <id>resolution.releases</id>
            <username>${env.RESOLUTION_REPO_USERNAME}</username>
            <password>${env.RESOLUTION_REPO_PASSWORD}</password>
        </server>
        <server>
            <id>resolution.snapshots</id>
            <username>${env.RESOLUTION_REPO_USERNAME}</username>
            <password>${env.RESOLUTION_REPO_PASSWORD}</password>
        </server>
        <server>
            <id>resolution.public</id>
            <username>${env.RESOLUTION_REPO_USERNAME}</username>
            <password>${env.RESOLUTION_REPO_PASSWORD}</password>
        </server>
        <server>
            <id>thirdparty</id>
            <username>${env.RESOLUTION_REPO_USERNAME}</username>
            <password>${env.RESOLUTION_REPO_PASSWORD}</password>
        </server>
        <server>
            <id>proxy.atlassian</id>
            <username>${env.RESOLUTION_REPO_USERNAME}</username>
            <password>${env.RESOLUTION_REPO_PASSWORD}</password>
        </server>
        <server>
            <id>proxy.central</id>
            <username>${env.RESOLUTION_REPO_USERNAME}</username>
            <password>${env.RESOLUTION_REPO_PASSWORD}</password>
        </server>
    </servers>
</settings>